{
  "name": "push-artifact-azure-devops-task",
  "version": "1.1.2",
  "description": "The Azure DevOps task to push the artifact version through the flow in the artifactz.io from the Azure DevOps pipeline",
  "main": "index.js",
  "scripts": {
    "test": "tsc && mocha tests/_suite.js --timeout 10000 --reporter mocha-junit-reporter --reporter-option mochaFile=reports/test-result.xml"
  },
  "author": "Igor Kolomiyets <igor.kolomiyets@iktech.io>",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.9",
    "azure-pipelines-task-lib": "^5.0.0"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.13.1",
    "@types/q": "^1.5.4",
    "mocha-junit-reporter": "^2.0.2",
    "sync-request": "^6.1.0"
  }
}
